name: pages-auto-build-deploy
on:
  # workflow_dispatch: 
  push:
    branches:
      - gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.100.2'
          extended: true

      - name: Build Hugo
        run: hugo -b "https://yunmaoqu.github.io/" -e "production"

      - name: Deploy
        uses: peaceiris/actions-main@v3
        with:
          github_token: ${{ secrets.GH_PAGE_ACTION_KEY }}
          publish_dir: ./public
          commit_message: ${{ github.event.head_commit.message }}
